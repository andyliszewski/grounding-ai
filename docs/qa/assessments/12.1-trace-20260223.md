# Requirements Traceability Matrix

## Story: 12.1 - Agentic Loop Infrastructure

### Coverage Summary

- Total Requirements: 8
- Fully Covered: 8 (100%)
- Partially Covered: 0 (0%)
- Not Covered: 0 (0%)

### Requirement Mappings

#### AC1: Ollama API called with `tools` parameter when agentic mode is enabled

**Coverage: FULL**

Given-When-Then Mappings:

- **Unit Test**: `TestQueryOllamaWithTools::test_sends_correct_payload`
  - Given: Messages and tool schema
  - When: query_ollama_with_tools is called
  - Then: Payload includes model, messages, tools, and stream=False

---

#### AC2: Tool calls in LLM response are detected via `tool_calls` array

**Coverage: FULL**

Given-When-Then Mappings:

- **Unit Test**: `TestParseToolCalls::test_parse_single_tool_call`
  - Given: Response with tool_calls array
  - When: parse_tool_calls is called
  - Then: ToolCall objects are returned with name and arguments

- **Unit Test**: `TestRunAgenticLoop::test_single_tool_call_executed`
  - Given: LLM returns tool call then final response
  - When: run_agentic_loop executes
  - Then: Tool call is detected and executed

---

#### AC3: Agent loop continues processing until no more tool calls returned

**Coverage: FULL**

Given-When-Then Mappings:

- **Unit Test**: `TestRunAgenticLoop::test_no_tool_calls_returns_immediately`
  - Given: LLM returns direct response (no tool calls)
  - When: run_agentic_loop executes
  - Then: Loop completes in 1 iteration with stopped_reason="complete"

- **Unit Test**: `TestRunAgenticLoop::test_single_tool_call_executed`
  - Given: LLM returns tool call, then final response
  - When: run_agentic_loop executes
  - Then: Loop completes in 2 iterations

---

#### AC4: Max iterations safeguard prevents infinite loops (configurable, default: 5)

**Coverage: FULL**

Given-When-Then Mappings:

- **Unit Test**: `TestDataclasses::test_agentic_config_defaults`
  - Given: Default AgenticConfig
  - When: Created without arguments
  - Then: max_iterations is 5

- **Unit Test**: `TestRunAgenticLoop::test_max_iterations_enforced`
  - Given: LLM always returns tool calls, max_iterations=3
  - When: run_agentic_loop executes
  - Then: Loop stops at 3, stopped_reason="max_iterations", note appended

---

#### AC5: Errors during tool execution are returned as tool result content (not raised as exceptions)

**Coverage: FULL**

Given-When-Then Mappings:

- **Unit Test**: `TestRunAgenticLoop::test_tool_execution_error_handled`
  - Given: Tool executor that raises ValueError
  - When: Tool is called during loop
  - Then: Error captured as string, loop continues, no exception raised

- **Unit Test**: `TestToolRegistry::test_execute_unknown_tool`
  - Given: Unknown tool name
  - When: ToolRegistry.execute is called
  - Then: Returns "Unknown tool: {name}" string, no exception

---

#### AC6: Message history correctly accumulates assistant and tool messages

**Coverage: FULL**

Given-When-Then Mappings:

- **Unit Test**: `TestRunAgenticLoop::test_message_history_accumulates`
  - Given: System prompt and user query
  - When: Loop executes tool call and second iteration
  - Then: Second call includes system, user, assistant, and tool messages

- **Unit Test**: `TestRunAgenticLoop::test_system_prompt_included`
  - Given: System prompt provided
  - When: run_agentic_loop called
  - Then: First message is system role with prompt

---

#### AC7: Final response extracted and returned to caller

**Coverage: FULL**

Given-When-Then Mappings:

- **Unit Test**: `TestRunAgenticLoop::test_no_tool_calls_returns_immediately`
  - Given: LLM returns content without tool calls
  - When: run_agentic_loop completes
  - Then: content field contains final response text

- **Unit Test**: `TestRunAgenticLoop::test_max_iterations_enforced`
  - Given: Loop hits max iterations
  - When: run_agentic_loop completes
  - Then: content contains last response + iteration note

---

#### AC8: Loop handles both single and multiple tool calls per turn

**Coverage: FULL**

Given-When-Then Mappings:

- **Unit Test**: `TestParseToolCalls::test_parse_multiple_tool_calls`
  - Given: Response with 2 tool calls
  - When: parse_tool_calls is called
  - Then: Returns 2 ToolCall objects with correct arguments

- **Unit Test**: `TestRunAgenticLoop::test_multiple_tool_calls_per_turn`
  - Given: LLM returns 2 tool calls in one response
  - When: run_agentic_loop executes
  - Then: Both tool calls executed, tool_calls_made contains 2 items

---

### Critical Gaps

None identified. All acceptance criteria have full test coverage.

### Test Design Quality

| Level       | Count | Notes                                    |
|-------------|-------|------------------------------------------|
| Unit        | 25    | Comprehensive coverage with mocking      |
| Integration | 0     | Future: Real Ollama API tests            |
| E2E         | 0     | Future: Full pipeline with search_corpus |

### Risk Coverage

| Risk ID   | Test Coverage                                    |
|-----------|--------------------------------------------------|
| TECH-001  | Partial - single tool per turn tested            |
| PERF-001  | Unit tested with mocks, no real network tests    |
| DATA-001  | Full - `test_parse_tool_call_args_as_string`     |
