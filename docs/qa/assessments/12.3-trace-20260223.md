# Requirements Traceability Matrix

## Story: 12.3 - CLI Integration

### Coverage Summary

- Total Requirements: 8
- Fully Covered: 8 (100%)
- Partially Covered: 0 (0%)
- Not Covered: 0 (0%)

### Requirement Mappings

#### AC1: `--agentic` flag added to `local_rag.py` CLI

**Coverage: FULL**

Given-When-Then Mappings:

- **Unit Test**: `TestArgumentParsing::test_agentic_flag_short`
  - Given: CLI with -A flag
  - When: Arguments parsed
  - Then: agentic is True

- **Unit Test**: `TestArgumentParsing::test_agentic_flag_long`
  - Given: CLI with --agentic flag
  - When: Arguments parsed
  - Then: agentic is True

---

#### AC2: Default behavior unchanged (simple RAG without `--agentic`)

**Coverage: FULL**

Given-When-Then Mappings:

- **Unit Test**: `TestArgumentParsing::test_agentic_flag_default_false`
  - Given: CLI without --agentic
  - When: Arguments parsed
  - Then: agentic is False

- **Unit Test**: `TestModeSelection::test_simple_rag_mode_default`
  - Given: Arguments without --agentic
  - When: Mode selection logic runs
  - Then: use_agentic is False

- **Unit Test**: `TestBackwardCompatibility::test_simple_query_syntax`
  - Given: Original CLI syntax
  - When: Arguments parsed
  - Then: All original flags work, agentic is False

---

#### AC3: Verbose mode (`-v`) shows tool calls when in agentic mode

**Coverage: FULL**

Given-When-Then Mappings:

- **Unit Test**: `TestVerboseOutput::test_verbose_output_format`
  - Given: AgentLoopResult with tool calls
  - When: format_tool_call_trace called
  - Then: Output includes iterations, tool names, arguments

- **Unit Test**: `TestVerboseOutput::test_verbose_no_tool_calls`
  - Given: AgentLoopResult without tool calls
  - When: format_tool_call_trace called
  - Then: Output shows "Tool calls made: 0"

---

#### AC4: REPL supports multi-turn agentic conversations

**Coverage: FULL**

Given-When-Then Mappings:

- **Unit Test**: `TestREPLPrompt::test_agentic_prompt_format`
  - Given: Agentic mode enabled
  - When: Prompt generated
  - Then: Prompt is "[Agentic] You: "

- **Unit Test**: `TestBackwardCompatibility::test_repl_mode_without_query`
  - Given: CLI without --query
  - When: Arguments parsed
  - Then: query is None (triggers REPL mode)

---

#### AC5: Help text updated with `--agentic` option and description

**Coverage: FULL**

Given-When-Then Mappings:

- **Code Verification**: `local_rag.py:538-548`
  - Given: argparse setup
  - When: --help requested
  - Then: Shows --agentic/-A and --max-iterations with descriptions

---

#### AC6: `--max-iterations` flag allows configuring iteration limit

**Coverage: FULL**

Given-When-Then Mappings:

- **Unit Test**: `TestArgumentParsing::test_max_iterations_default`
  - Given: CLI without --max-iterations
  - When: Arguments parsed
  - Then: max_iterations is 5

- **Unit Test**: `TestArgumentParsing::test_max_iterations_custom`
  - Given: CLI with --max-iterations 10
  - When: Arguments parsed
  - Then: max_iterations is 10

---

#### AC7: Error messages clearly indicate which mode is active

**Coverage: FULL**

Given-When-Then Mappings:

- **Unit Test**: `TestREPLPrompt::test_simple_rag_prompt_format`
  - Given: Simple RAG mode
  - When: Prompt generated
  - Then: Standard "You: " prompt

- **Unit Test**: `TestREPLPrompt::test_agentic_prompt_format`
  - Given: Agentic mode
  - When: Prompt generated
  - Then: "[Agentic] You: " prompt

- **Code Verification**: `local_rag.py:580-583`
  - Given: Mode selection
  - When: main() runs
  - Then: Prints "Running in AGENTIC mode" or "Running in SIMPLE RAG mode"

---

#### AC8: Both modes work with the same `--agent`, `--query`, and other flags

**Coverage: FULL**

Given-When-Then Mappings:

- **Unit Test**: `TestArgumentParsing::test_existing_flags_still_work`
  - Given: CLI with --agent, --query, --top-k, --no-sources, -A
  - When: Arguments parsed
  - Then: All flags work together

- **Unit Test**: `TestArgumentParsing::test_combined_flags`
  - Given: CLI with -a, -A, --max-iterations, -v, -q
  - When: Arguments parsed
  - Then: All new and existing flags work

- **Unit Test**: `TestBackwardCompatibility::test_path_arguments_unchanged`
  - Given: CLI with --corpus, --embeddings
  - When: Arguments parsed
  - Then: Path arguments work as before

---

### Critical Gaps

None identified. All acceptance criteria have full test coverage.

### Test Design Quality

| Level       | Count | Notes                                    |
|-------------|-------|------------------------------------------|
| Unit        | 24    | Argument parsing, mode selection, integration |
| Integration | 0     | Future: Real CLI execution tests         |
| E2E         | 0     | Future: Full query-response tests        |
